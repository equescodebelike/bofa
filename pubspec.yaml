name: bachelor_of_alcoholism
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_static: ^1.1.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  shelf_router_generator: ^1.1.0
  build_runner: ^2.4.13 
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
